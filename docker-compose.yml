services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: aslan
      POSTGRES_PASSWORD: aslan
      POSTGRES_DB: aslan
    ports:
      - "5432:5432"
    volumes:
      - aslan_pg:/var/lib/postgresql/data

volumes:
  aslan_pg:
